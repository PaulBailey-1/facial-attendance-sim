#include <iostream>

#include "Simulation.h"
#include "Display.h"

int main() {

    Simulation sim;
    while (1) {}
    //DBConnection db; 
    //if (!db.connect()) {
    //    return 1;
    //}
    //
    //Device* dev = new Device();
    //dev->run();

    return 0;
}